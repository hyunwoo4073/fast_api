version: "3"

services:
  api:
    build: .
    image: my-fastapi-image:latest
    ports:
      - "8000:8000"
    env_file:
      - .env.prod

  database:
    image: mongodb/mongodb-community-server:latest
    ports:
      - "27017:27017"
    volumes:
      - data:/data/db

volumes:
  data: